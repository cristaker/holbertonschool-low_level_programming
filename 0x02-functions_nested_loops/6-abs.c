#include "main.h"
/**
 * _abs - protitype for return absolute value.
 * @i: imput integer
 * Return: Absolute value
 * **/
int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
